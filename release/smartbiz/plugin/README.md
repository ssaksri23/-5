# plugin/

이 폴더는 향후 추가 기능(플러그인)을 위해 예약된 폴더입니다. 현재 버전(1.0.0)에는
포함된 추가 기능이 없습니다.

앞으로 테마 코어를 수정하지 않고 추가할 수 있는 기능(예: 예약 신청, 견적 계산기 등)은
이 폴더 아래에 `plugin/<기능명>/` 형태로 추가되며, 각 기능은 그누보드5의
`extend/` 자동 로드 방식과 동일한 원리로 코어 파일 수정 없이 동작하도록 설계됩니다.
